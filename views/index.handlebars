<h1>Burgers</h1>

<h2>Burgers waiting to be devoured</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devoured?</label><br>
    <input id = "radio_1" type="radio" name="devoured" value="0" checked> Not devoured!<br>
    <input id = "radio_2" type="radio" name="devoured" value="1" > Devoured
  </div>

  <button type="submit">Add Burger</button>
</form>